[build]
  command = "pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"

# Single-page app: serve index.html for client-side (wouter) routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
